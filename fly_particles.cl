__kernel
void fly_particles() {
  // Figure this out later.
  // Load a buffer with x and v values of float2 (vector object??)
  // Fly across the particles using typical algorithm
  // Implement a dt to pass through as a constant
  // Return a vector of all the particles New X and New V
}
